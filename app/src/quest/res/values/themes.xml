<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Panel theme for Spatial SDK panels, passed via PanelStyleOptions(themeResourceId = …).

      The window background must be transparent: the panel's Android surface is composited onto a
      quad in the 3D scene, and an opaque window background paints the full quad including the
      corners a rounded panel shape is supposed to cut away. Backgrounds are painted by Compose
      instead (PuckTriviaPanel), which respects the panel's shape.
    -->
    <style name="Theme.PuckTrivia.Panel" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
